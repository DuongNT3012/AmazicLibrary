pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
//    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        flatDir {
            dirs new File(rootDir, 'mylibrary/libs')
        }
//        flatDir { dirs 'libs' }
        maven {
            url 'https://jitpack.io'
        }
    }
}

rootProject.name = "AmazicLibrary"
include ':app'
include ':mylibrary'
include ':customevent'
include ':sdk'